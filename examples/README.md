# 示例目录

本目录包含各种任务模式的示例。

## 目录结构

- `latex_only/` - 仅 LaTeX 排版示例
- `translation_only/` - 仅翻译示例
- `translation_latex/` - 翻译 + LaTeX 排版示例
- `template_migration/` - 模板迁移示例
- `complex_paper/` - 复杂论文完整处理示例

## 使用方法

每个示例目录包含：
- `input/` - 输入文件
- `README.md` - 示例说明

`output/` 目录在运行时自动生成，不包含在示例中。
